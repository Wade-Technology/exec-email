{
  "exec-email": {
    "version": "1.3.0",
    "updated": "2026-04-26",
    "description": "Replace aioimaplib (login() hangs against Migadu, all execs stuck NONAUTH) with imap-tools polling. Sync IMAP run via asyncio.to_thread(). Polling interval POLL_INTERVAL_FALLBACK (default 30s). All 20 unit tests pass."
  }
}
